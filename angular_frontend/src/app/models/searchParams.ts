export interface params {
    "destination": string,
    "check-in": Date,
    "check-out": Date,
    "number_of_guests": number,
    "free_only": boolean,
    "services": string[],
    "order_by": string
}